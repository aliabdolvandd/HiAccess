import { Typography } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <Typography variant="h1" sx={{ pt: 15 }}>
      cart
    </Typography>
  );
};

export default page;
